/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The library code lives in src/. Next.js routes live in app/.
  // No special transpile / experimental flags needed; both are first-class TS.
}

export default nextConfig
